# Maintainer: Nicolas Iooss (nicolas <dot> iooss <at> m4x <dot> org)
#
# This PKGBUILD is maintained on https://github.com/archlinuxhardened/selinux.
# If you want to help keep it up to date, please open a Pull Request there.

pkgname=selinux-dbus-config
_pkgname=selinux-dbus
_pkgver=3.1-rc1
pkgver=${_pkgver/-/}
pkgrel=1
pkgdesc="DBus service which allows managing SELinux configuration"
groups=('selinux')
arch=('any')
url='https://github.com/SELinuxProject/selinux/wiki'
license=('GPL2')
depends=('python' 'selinux-python')
conflicts=('policycoreutils<2.7')
source=("https://github.com/SELinuxProject/selinux/releases/download/20200518/${_pkgname}-${_pkgver}.tar.gz")
sha256sums=('b4d532bc1ddcbc5c10fe046309f3df81f71818070602ec990e546c6c258e677a')

build() {
  cd "${_pkgname}-${_pkgver}"
  make
}

package() {
  cd "${_pkgname}-${_pkgver}"
  make DESTDIR="${pkgdir}" install
}
